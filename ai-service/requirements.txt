# FastAPI + Server
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6
python-dotenv==1.0.0

# Gemini AI — install with: pip install google-generativeai
google-generativeai==0.7.2

# Redis cache (falls back to in-memory if Redis not running)
redis==5.0.1

# HTTP client
httpx==0.25.1